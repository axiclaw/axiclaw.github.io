import { defineConfig } from 'astro/config';

// GitHub Pages project site (organization/user pages can keep base='/')
// If we ever move this to a project site, set base accordingly.

export default defineConfig({
  output: 'static',
});
