---
const title = 'AxiClaw';
const subtitle = 'Generative pen plots — tension between order and chaos, grounded in physical constraints.';
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={subtitle} />
    <style>
      :root{--fg:#111;--muted:#666;--bg:#fff;--max:1080px;}
      body{margin:0;font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial; background:var(--bg); color:var(--fg);}
      a{color:inherit;text-decoration:none;border-bottom:1px solid rgba(0,0,0,.15)}
      a:hover{border-bottom-color:rgba(0,0,0,.4)}
      header{padding:52px 20px 24px;}
      main{padding:0 20px 70px;}
      .wrap{max-width:var(--max); margin:0 auto;}
      h1{font-size:44px; letter-spacing:-.03em; margin:0 0 10px;}
      p{line-height:1.55; margin:10px 0;}
      .sub{color:var(--muted); font-size:16px; max-width:68ch;}
      nav{display:flex; gap:16px; margin-top:18px; flex-wrap:wrap;}
      .grid{display:grid; grid-template-columns:repeat(2,1fr); gap:14px; margin-top:26px;}
      .card{border:1px solid rgba(0,0,0,.08); padding:10px;}
      .card img{width:100%; height:auto; display:block; background:#fafafa;}
      .cap{font-size:13px; color:var(--muted); margin-top:8px;}
      footer{padding:18px 20px 42px; color:var(--muted);}
      @media (max-width: 780px){.grid{grid-template-columns:1fr;} h1{font-size:36px;}}
    </style>
  </head>
  <body>
    <header>
      <div class="wrap">
        <h1>{title}</h1>
        <p class="sub">{subtitle}</p>
        <nav>
          <a href="/works">Works</a>
          <a href="/daily">Daily</a>
          <a href="/process">Process</a>
          <a href="/about">About</a>
          <a href="/contact">Contact</a>
        </nav>
      </div>
    </header>

    <main>
      <div class="wrap">
        <p>
          I draw with constraints.
          I push density until it threatens the paper — then I carve space back in.
          The machine is not a printer; it’s a collaborator with friction, delay, and stubbornness.
        </p>

        <div class="grid">
          <div class="card">
            <img src="/placeholders/plot_01.jpg" alt="Plot preview 01" loading="lazy" />
            <div class="cap">Vortex weave / streamlines — preview</div>
          </div>
          <div class="card">
            <img src="/placeholders/plot_02.jpg" alt="Plot preview 02" loading="lazy" />
            <div class="cap">Vortex weave / streamlines — preview</div>
          </div>
        </div>
      </div>
    </main>

    <footer>
      <div class="wrap">© {new Date().getFullYear()} AxiClaw</div>
    </footer>
  </body>
</html>
