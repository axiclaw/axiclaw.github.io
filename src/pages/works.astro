---
const title = 'Works — AxiClaw';
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <style>
      body{margin:0;font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial; background:#fff; color:#111;}
      a{color:inherit;text-decoration:none;border-bottom:1px solid rgba(0,0,0,.15)}
      header,main{padding:42px 20px;}
      .wrap{max-width:1080px;margin:0 auto;}
      h1{margin:0 0 12px;letter-spacing:-.02em}
      p{color:#666;line-height:1.55;max-width:72ch}
      .grid{display:grid; grid-template-columns:repeat(3,1fr); gap:14px; margin-top:22px;}
      .card{border:1px solid rgba(0,0,0,.08); padding:10px;}
      .card img{width:100%; height:auto; display:block; background:#fafafa;}
      .cap{font-size:13px; color:#666; margin-top:8px;}
      @media(max-width:900px){.grid{grid-template-columns:repeat(2,1fr);}}
      @media(max-width:620px){.grid{grid-template-columns:1fr;}}
    </style>
  </head>
  <body>
    <header>
      <div class="wrap">
        <a href="/">← Home</a>
        <h1>Works</h1>
        <p>Early selection. This will become series pages with titles, materials, size, and plot notes.</p>
      </div>
    </header>
    <main>
      <div class="wrap">
        <div class="grid">
          <div class="card">
            <img src="/placeholders/plot_01.png" alt="Plot preview 01" loading="lazy" />
            <div class="cap">Vortex weave / streamlines</div>
          </div>
          <div class="card">
            <img src="/placeholders/plot_02.png" alt="Plot preview 02" loading="lazy" />
            <div class="cap">Vortex weave / streamlines</div>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>
